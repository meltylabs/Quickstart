{
  "name": "biologix-public-intel-vercel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check api/cron.js && node --check 'api/biologix-intel/[route].js' && node --check lib/*.js"
  },
  "dependencies": {
    "@vercel/blob": "2.6.1"
  },
  "engines": {
    "node": ">=20"
  }
}
