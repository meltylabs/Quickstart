// Copy this file to admin/config.js and set a strong password.
// config.js is gitignored — the password is never committed to the repo.
// For production use, enforce access at the server level instead
// (Netlify password protection, HTTP Basic Auth, etc.).
window.ADMIN_PASSWORD = 'your-password-here';
